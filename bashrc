alias ghp='ghp-import -m "Generate Pelican site" --no-jekyll -b master output'

alias ppub='pelican content -o output -s publishconf.py'

git config --global user.email "lifebalance@gmail.com"

git config --global user.name kgashok
